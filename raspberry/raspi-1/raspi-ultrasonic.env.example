# Example environment file for raspi-ultrasonic.service
# Copy to /etc/default/raspi-ultrasonic and edit values as needed, then systemctl daemon-reload and enable the service.

# Full URL to post bin updates. If you provide just the backend base URL the client will append /api/bin/update
BIN_UPDATE_URL="https://e-waste-backend-3qxc.onrender.com/api/bin/update"

# Interval between sensor reads in seconds
UPDATE_INTERVAL=10

# Optional: JSON array describing bins and pins (BCM numbering). Example:
# BIN_CONFIG_JSON='[{"id":"bin-1","trigger":23,"echo":24,"empty_distance_cm":80.0,"full_distance_cm":10.0}]'
# If set, the script will parse this JSON; otherwise it uses the defaults in the repo.
BIN_CONFIG_JSON='[{"id":"bin-1","trigger":23,"echo":24,"empty_distance_cm":80.0,"full_distance_cm":10.0}]'
